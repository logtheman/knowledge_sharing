<h2> New Answer </h2>

<%= form_for(answer) do |a| %>
	
	<div class="form-group">
		<div class="field">
			<%= a.label :response %><br />
			<%= a.text_area :response	 %>
		</div>
	</div>
	<div class="form-group">
		<div class="field">
			<%= a.submit %>
		</div>
	</div>

<% end %>